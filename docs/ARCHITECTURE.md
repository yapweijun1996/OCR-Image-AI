# Architecture

High-level shape of the OCR demo. Read this before changing how the pieces fit together.

## File map

```
.
├── index.html        # thin markup shell (~180 lines); inline FOUC theme script only
├── manifest.json     # PWA manifest
├── sw.js             # service worker (cache + offline fallback)
├── offline.html      # fallback page when navigation fails offline
├── icon.svg          # single app icon (any + maskable purposes)
│
├── assets/
│   ├── styles.css        # all CSS, 17 banner-commented sections
│   └── icons/            # PNG icons regenerated by scripts/generate-icons.py
│       ├── icon-192.png              (any)
│       ├── icon-512.png              (any)
│       ├── icon-maskable-512.png     (maskable, content in safe zone)
│       └── apple-touch-icon-180.png  (linked from <head>)
│
├── js/
│   ├── main.js       # boot — calls every init() in dep order, then renderHistory()
│   ├── fouc.js       # sync (non-module) script: sets data-theme before CSS paints
│   ├── utils.js      # $, fmtBytes, fmtTime, escapeHtml, setStatus, haptic
│   ├── db.js         # IndexedDB wrapper (dbAdd / dbAll / dbDelete / dbClear)
│   ├── config.js     # XOR-decoded key, LS keys, loadConfig / saveConfig / getConfig
│   ├── theme.js      # data-theme management + theme-color sync
│   ├── image.js      # downscale, current state, drop/paste/pick, share-target consume
│   ├── history.js    # renderHistory, Re-run / Copy / Delete row actions, JSON export
│   ├── api.js        # streaming SSE client, runOCR, Stop button, AbortController
│   ├── result.js     # Copy / Download / Share buttons for the output panel
│   ├── modal.js      # generic open/close (modal currently dormant)
│   ├── pwa.js        # SW registration, update banner, install prompt
│   └── prompts.js    # English-only prompt presets (general/receipt/handwriting/…)
│
├── scripts/
│   └── generate-icons.py    # Pillow-based PNG icon generator (Phase 9)
│
├── README.md
├── CONTRIBUTING.md
├── CHANGELOG.md
├── .gitignore
├── .lighthouserc.json       # Lighthouse CI thresholds (Phase 10)
├── .github/
│   └── workflows/
│       ├── deploy.yml       # GitHub Pages auto-deploy on push to main
│       └── lighthouse.yml   # Lighthouse CI on PRs + main
└── docs/
    ├── ARCHITECTURE.md   (this file)
    ├── API.md
    ├── PWA.md
    ├── DESIGN.md
    ├── DEVELOPMENT.md
    └── REFACTOR-PLAN.md
```

The split happened in one pass on 2026-05-12 — see [`REFACTOR-PLAN.md`](REFACTOR-PLAN.md). All phases shipped (0 – 10) except the still-listed nice-to-haves (iOS splashes, file_handlers, Playwright). Constraints that still hold: no bundler, no transpiler, no framework, native ES modules only.

## Module map

Every JS file exports a single `init<Name>()` function (idempotent, safe to call once after DOM is ready) plus its public API.

| File           | Public exports                                       | Imports from                                |
|----------------|------------------------------------------------------|---------------------------------------------|
| `utils.js`     | `$`, `fmtBytes`, `fmtTime`, `escapeHtml`, `setStatus`, `haptic` | (none)                              |
| `db.js`        | `dbAdd`, `dbAll`, `dbDelete`, `dbClear`              | (none)                                      |
| `config.js`    | `LS`, `loadConfig`, `saveConfig`, `getConfig`, `initConfig` | `utils`                              |
| `theme.js`     | `applyTheme`, `initTheme`                            | `utils`, `config`                           |
| `image.js`     | `getCurrent`, `loadFromHistory`, `clearCurrent`, `initImage`, `handleFile` | `utils`              |
| `history.js`   | `renderHistory`, `initHistory`                       | `utils`, `db`, `image`                      |
| `api.js`       | `runOCR`, `initApi`                                  | `utils`, `config`, `image`, `db`, `history` |
| `result.js`    | `initResult`                                         | `utils`                                     |
| `modal.js`     | `closeModal`, `initModal`                            | `utils`                                     |
| `pwa.js`       | `initPwa`                                            | `utils`                                     |
| `main.js`      | (entry point — no exports)                           | every `init*` above                         |

No circular imports. Single direction of data flow.

## Data flow

```
            ┌─────────────────────────────────────┐
            │   User picks / drops / pastes image │
            └────────────────┬────────────────────┘
                             │
                             ▼
             ┌─────────────────────────────┐
             │  FileReader → data URL      │
             │  Canvas downscale (≤1600px) │
             └────────────────┬────────────┘
                              │  current = { dataUrl, name, size, w, h }
                              ▼
             ┌──────────────────────────────┐
User clicks  │  runOCR()                    │  ◄── prompt + effort from UI
"Recognize"  │  POST /v1/responses          │
─────────────►│  stream: true                │
             └─┬────────────────────────────┘
               │ fetch().body (ReadableStream)
               ▼
             ┌──────────────────────────────┐
             │  readSSE()                   │
             │  parse data: <json> events   │
             │  on delta → append to DOM    │  ──► result panel
             │  on completed → record usage │
             └─┬────────────────────────────┘
               │ accumulated text
               ▼
             ┌──────────────────────────────┐
             │  IndexedDB.add(record)       │
             │  renderHistory()             │
             └──────────────────────────────┘
```

## State summary

| State                          | Where it lives          | Lifetime           |
|--------------------------------|-------------------------|--------------------|
| Theme (`light` / `dark` / auto)| `localStorage`          | Until user clears  |
| Prompt, effort                 | `localStorage`          | Until user clears  |
| Hidden config (endpoint, key, model) | DOM input values (hardcoded) | Session — never persisted from UI |
| Current image (`current`)      | JS variable             | Until cleared      |
| AbortController (`abortCtrl`)  | JS variable             | One run            |
| History records                | IndexedDB `ocr-db.records` | Until user clears |
| Cached PWA shell               | Cache Storage `ocr-<VERSION>` | Until SW activates new VERSION |

## External dependencies

- **`gpt.yapweijun1996.com/v1/responses`** — the OpenAI-compatible Responses API gateway. See [`API.md`](API.md).
- **Fonts** — system fonts only. No webfont request goes out.
- **No JS libs** — zero runtime deps.

## Boundaries that must not be crossed

1. **The service worker MUST NOT intercept non-GET requests.** Streaming POSTs go through `fetch`, and a SW that tries to cache or proxy them would break the stream. The guard `if (req.method !== 'GET') return;` is load-bearing.
2. **The hardcoded API key is XOR-obfuscated, not encrypted.** Anyone can decode it. Treat the demo gateway key as already-leaked.
3. **No data URLs in history bigger than ~5 MB.** IndexedDB has a per-database quota; storing many full-resolution images quickly hits it. The 1600 px downscale in `downscale()` is what keeps records small.
4. **Theme-color meta must stay in sync with the `--bg` of the active theme** so the browser chrome (Android URL bar, iOS status bar) doesn't look mismatched.

## Adding a feature — where does it go?

| You want to…                          | Edit                                                              |
|---------------------------------------|-------------------------------------------------------------------|
| Add a new config knob                 | `index.html` (new `<div class="list-row">`) + `LS` in `js/config.js` + add to `loadConfig` / `saveConfig` / `getConfig` |
| Tweak the streaming output rendering  | `readSSE` callback inside `runOCR()` in `js/api.js`               |
| Add a new history-row action          | Row template inside `renderHistory()` + click handler in `initHistory()`, both in `js/history.js` |
| Theme a new component                 | `:root` tokens in `assets/styles.css` (section 1), then the component's section |
| Cache a new asset for offline use     | `PRECACHE` array in [`sw.js`](../sw.js) + bump `VERSION`          |
| Add a new module                      | Drop a file in `js/`, export `init<Name>()`, import + call in `js/main.js` |
